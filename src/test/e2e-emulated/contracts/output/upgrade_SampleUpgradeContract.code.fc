#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; upgrade_SampleUpgradeContract.headers.fc
;;
;; Header files for SampleUpgradeContract
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_not_null
forall X -> X __tact_not_null(X x) impure inline;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; $Upgrade$_load_without_opcode
(slice, ((cell, cell))) $Upgrade$_load_without_opcode(slice sc_0) inline;

;; $SampleUpgradeContract$_store
builder $SampleUpgradeContract$_store(builder build_0, (int, slice, int) v) inline;

;; $SampleUpgradeContract$_load
(slice, ((int, slice, int))) $SampleUpgradeContract$_load(slice sc_0) inline;

;; $SampleUpgradeContract$init$_load
(slice, ((slice))) $SampleUpgradeContract$init$_load(slice sc_0) inline;

;; $SampleUpgradeContract$_contract_init
(int, slice, int) $SampleUpgradeContract$_contract_init(slice $owner) impure inline;

;; $SampleUpgradeContract$_contract_load
(int, slice, int) $SampleUpgradeContract$_contract_load() impure inline;

;; $SampleUpgradeContract$_contract_store
() $SampleUpgradeContract$_contract_store((int, slice, int) v) impure inline;

;; $SampleUpgradeContract$_fun_add
((int, slice, int), ()) $SampleUpgradeContract$_fun_add((int, slice, int) $self, int $v) impure inline_ref;

;; $SampleUpgradeContract$_fun_counter
((int, slice, int), int) $SampleUpgradeContract$_fun_counter((int, slice, int) $self) impure inline_ref;

;; $SampleUpgradeContract$_fun_requireOwner
((int, slice, int), ()) $SampleUpgradeContract$_fun_requireOwner((int, slice, int) $self) impure inline_ref;

;; $SampleUpgradeContract$_fun_validateUpgrade
((int, slice, int), ()) $SampleUpgradeContract$_fun_validateUpgrade((int, slice, int) $self, (int, slice, int, slice) $_, (cell, cell) $__) impure inline;

;; $SampleUpgradeContract$_fun_upgrade
((int, slice, int), ()) $SampleUpgradeContract$_fun_upgrade((int, slice, int) $self, (int, slice, int, slice) $_, (cell, cell) $msg) impure inline;

;; $SampleUpgradeContract$_fun_isUpgradable
((int, slice, int), int) $SampleUpgradeContract$_fun_isUpgradable((int, slice, int) $self) impure inline_ref;

;; $SampleUpgradeContract$_fun_version
((int, slice, int), int) $SampleUpgradeContract$_fun_version((int, slice, int) $self) impure inline_ref;

;; $SampleUpgradeContract$_fun_owner
((int, slice, int), slice) $SampleUpgradeContract$_fun_owner((int, slice, int) $self) impure inline_ref;


;; upgrade_SampleUpgradeContract.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

forall X -> X __tact_not_null(X x) impure inline {
    throw_if(128, null?(x)); return x;
}

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

((int, slice, int), ()) $SampleUpgradeContract$_fun_requireOwner((int, slice, int) $self) impure inline_ref {
    var (($self'_version, $self'owner, $self'counter)) = $self;
    throw_unless(132, ( equal_slices_bits($self'owner, __tact_context_get_sender()) ));
    return (($self'_version, $self'owner, $self'counter), ());
}

((int, slice, int), slice) $SampleUpgradeContract$_fun_owner((int, slice, int) $self) impure inline_ref {
    var (($self'_version, $self'owner, $self'counter)) = $self;
    var $fresh$ret_2563 = $self'owner;
    return (($self'_version, $self'owner, $self'counter), $fresh$ret_2563);
}

;; upgrade_SampleUpgradeContract.storage.fc
;;
;; Type: Upgrade
;; Header: 0xdcbd3cd5
;; TLB: upgrade#dcbd3cd5 code:Maybe ^cell data:Maybe ^cell = Upgrade
;;

(slice, ((cell, cell))) $Upgrade$_load_without_opcode(slice sc_0) inline {
    var v'code = sc_0~load_int(1) ? sc_0~load_ref() : null();
    var v'data = sc_0~load_int(1) ? sc_0~load_ref() : null();
    return (sc_0, (v'code, v'data));
}

;;
;; Type: SampleUpgradeContract
;; TLB: _ _version:uint32 owner:address counter:uint32 = SampleUpgradeContract
;;

builder $SampleUpgradeContract$_store(builder build_0, (int, slice, int) v) inline {
    var (v'_version, v'owner, v'counter) = v;
    build_0 = build_0.store_uint(v'_version, 32);
    build_0 = build_0.store_slice(v'owner);
    build_0 = build_0.store_uint(v'counter, 32);
    return build_0;
}

(slice, ((int, slice, int))) $SampleUpgradeContract$_load(slice sc_0) inline {
    var v'_version = sc_0~load_uint(32);
    var v'owner = sc_0~load_msg_addr();
    var v'counter = sc_0~load_uint(32);
    return (sc_0, (v'_version, v'owner, v'counter));
}

(slice, ((slice))) $SampleUpgradeContract$init$_load(slice sc_0) inline {
    var v'owner = sc_0~load_msg_addr();
    return (sc_0, (v'owner));
}

(int, slice, int) $SampleUpgradeContract$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$SampleUpgradeContract$_load();
    } else {
        (slice $owner) = $sc~$SampleUpgradeContract$init$_load();
        $sc.end_parse();
        return $SampleUpgradeContract$_contract_init($owner);
    }
}

() $SampleUpgradeContract$_contract_store((int, slice, int) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $SampleUpgradeContract$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract SampleUpgradeContract functions
;;

(int, slice, int) $SampleUpgradeContract$_contract_init(slice $owner) impure inline {
    var (($self'_version, $self'owner, $self'counter)) = (0, null(), null());
    $self'owner = $owner;
    $self'counter = 0;
    return ($self'_version, $self'owner, $self'counter);
}

() $global_setCode(cell $code) impure asm """
    SETCODE
""";

() $global_setData(cell $data) impure asm """
    c4 POP
""";

((int, slice, int), ()) $SampleUpgradeContract$_fun_add((int, slice, int) $self, int $v) impure inline_ref {
    var (($self'_version, $self'owner, $self'counter)) = $self;
    var ($ctx'bounceable, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
    throw_unless(4429, ( equal_slices_bits($self'owner, $ctx'sender) ));
    $self'counter = $self'counter + $v;
    return (($self'_version, $self'owner, $self'counter), ());
}

((int, slice, int), int) $SampleUpgradeContract$_fun_counter((int, slice, int) $self) impure inline_ref {
    var (($self'_version, $self'owner, $self'counter)) = $self;
    var $fresh$ret_2560 = $self'counter;
    return (($self'_version, $self'owner, $self'counter), $fresh$ret_2560);
}

((int, slice, int), ()) $SampleUpgradeContract$_fun_validateUpgrade((int, slice, int) $self, (int, slice, int, slice) $_, (cell, cell) $__) impure inline {
    var (($self'_version, $self'owner, $self'counter)) = $self;
    var (($_'bounceable, $_'sender, $_'value, $_'raw)) = $_;
    var (($__'code, $__'data)) = $__;
    ($self'_version, $self'owner, $self'counter)~$SampleUpgradeContract$_fun_requireOwner();
    return (($self'_version, $self'owner, $self'counter), ());
}

((int, slice, int), ()) $SampleUpgradeContract$_fun_upgrade((int, slice, int) $self, (int, slice, int, slice) $_, (cell, cell) $msg) impure inline {
    var (($self'_version, $self'owner, $self'counter)) = $self;
    var (($_'bounceable, $_'sender, $_'value, $_'raw)) = $_;
    var (($msg'code, $msg'data)) = $msg;
    if ((~ null?($msg'code))) {
        $global_setCode(__tact_not_null($msg'code));
    }
    if ((~ null?($msg'data))) {
        $global_setData(__tact_not_null($msg'data));
        throw(0);
    }
    return (($self'_version, $self'owner, $self'counter), ());
}

((int, slice, int), int) $SampleUpgradeContract$_fun_isUpgradable((int, slice, int) $self) impure inline_ref {
    var (($self'_version, $self'owner, $self'counter)) = $self;
    var $fresh$ret_2561 = true;
    return (($self'_version, $self'owner, $self'counter), $fresh$ret_2561);
}

((int, slice, int), int) $SampleUpgradeContract$_fun_version((int, slice, int) $self) impure inline_ref {
    var (($self'_version, $self'owner, $self'counter)) = $self;
    var $fresh$ret_2562 = $self'_version;
    return (($self'_version, $self'owner, $self'counter), $fresh$ret_2562);
}

;;
;; Get methods of a Contract SampleUpgradeContract
;;

_ %counter() method_id(104984) {
    var self = $SampleUpgradeContract$_contract_load();
    var res = self~$SampleUpgradeContract$_fun_counter();
    return res;
}

_ %isUpgradable() method_id(92955) {
    var self = $SampleUpgradeContract$_contract_load();
    var res = self~$SampleUpgradeContract$_fun_isUpgradable();
    return res;
}

_ %version() method_id(76407) {
    var self = $SampleUpgradeContract$_contract_load();
    var res = self~$SampleUpgradeContract$_fun_version();
    return res;
}

_ %owner() method_id(83229) {
    var self = $SampleUpgradeContract$_contract_load();
    var res = self~$SampleUpgradeContract$_fun_owner();
    return res;
}

;; message opcode reader utility
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";
;;
;; Routing of a Contract SampleUpgradeContract
;;

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'_version, $self'owner, $self'counter) = $SampleUpgradeContract$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) { return (); }
    int op = 0;
    int in_msg_length = slice_bits(in_msg);
    if (in_msg_length >= 32) {
        op = in_msg~load_uint(32);
        ;; Receive Upgrade message
        if (op == 3703389397) {
            var ($msg'code, $msg'data) = in_msg~$Upgrade$_load_without_opcode();
            var ($ctx'bounceable, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
            ($self'_version, $self'owner, $self'counter)~$SampleUpgradeContract$_fun_validateUpgrade(($ctx'bounceable, $ctx'sender, $ctx'value, $ctx'raw), ($msg'code, $msg'data));
            ($self'_version, $self'owner, $self'counter)~$SampleUpgradeContract$_fun_upgrade(($ctx'bounceable, $ctx'sender, $ctx'value, $ctx'raw), ($msg'code, $msg'data));
            $self'_version = $self'_version + 1;
            ;; Persist state
            $SampleUpgradeContract$_contract_store(($self'_version, $self'owner, $self'counter));
            return ();
        }
        
    }
    ;; Receive empty message
    if ((op == 0) & (in_msg_length <= 32)) {
        return ();
    }
    ;; Empty Receiver and Text Receivers
    var text_op = slice_hash(in_msg);
    ;; Receive "increment" message
    if (text_op == 0x99785e1361de95773685af32116e211933c29c96d195ce905c056195855e227c) {
        ($self'_version, $self'owner, $self'counter)~$SampleUpgradeContract$_fun_add(1);
        ;; Persist state
        $SampleUpgradeContract$_contract_store(($self'_version, $self'owner, $self'counter));
        return ();
    }
    ;; Throw if not handled
    throw(130);
}

() __tact_selector_hack_asm() impure asm """
@atend @ 1 {
    execute current@ context@ current!
    {
        }END> b>
        
        <{
            SETCP0 DUP
            IFNOTJMP:<{
                DROP over <s ref@ 0 swap @procdictkeylen idict@ { "internal shortcut error" abort } ifnot @addop
            }>
swap <s ref@
            0 swap @procdictkeylen idict- drop
            -1 swap @procdictkeylen idict- drop
            65535 swap @procdictkeylen idict- drop

            @procdictkeylen DICTPUSHCONST DICTIGETJMPZ 11 THROWARG
        }> b>
    } : }END>c
    current@ context! current!
} does @atend !
""";
() __tact_selector_hack() method_id(65535) {
    return __tact_selector_hack_asm();
}