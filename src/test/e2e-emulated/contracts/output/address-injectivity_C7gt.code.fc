#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; address-injectivity_C7gt.headers.fc
;;
;; Header files for C7gt
;; NOTE: declarations are sorted for optimal order
;;

;; $C7gt$_load
(slice, ((int))) $C7gt$_load(slice sc_0) inline;

;; $C7gt$init$_load
(slice, ((int))) $C7gt$init$_load(slice sc_0) inline;

;; $C7gt$_contract_init
(int) $C7gt$_contract_init(int $v) impure inline;

;; $C7gt$_contract_load
(int) $C7gt$_contract_load() impure inline_ref;

;; $C7gt$_fun_incrAndGetField1
((int), int) $C7gt$_fun_incrAndGetField1((int) $self, int $t) impure inline_ref;


;; address-injectivity_C7gt.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

;; address-injectivity_C7gt.storage.fc
;;
;; Type: C7gt
;;

(slice, ((int))) $C7gt$_load(slice sc_0) inline {
    var v'f1 = sc_0~load_int(257);
    return (sc_0, (v'f1));
}

(slice, ((int))) $C7gt$init$_load(slice sc_0) inline {
    var v'v = sc_0~load_int(257);
    return (sc_0, (v'v));
}

(int) $C7gt$_contract_load() impure inline_ref {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$C7gt$_load();
    } else {
        (int $v) = $sc~$C7gt$init$_load();
        $sc.end_parse();
        return $C7gt$_contract_init($v);
    }
}

;;
;; Contract C7gt functions
;;

(int) $C7gt$_contract_init(int $v) impure inline {
    var (($self'f1)) = (null());
    $self'f1 = $v;
    return ($self'f1);
}

((int), int) $C7gt$_fun_incrAndGetField1((int) $self, int $t) impure inline_ref {
    var (($self'f1)) = $self;
    var $fresh$ret_84 = ($self'f1 + $t);
    return (($self'f1), $fresh$ret_84);
}

;;
;; Get methods of a Contract C7gt
;;

_ %incrAndGetField1(int $t) method_id(111350) {
    int $t = $t;
    var self = $C7gt$_contract_load();
    var res = self~$C7gt$_fun_incrAndGetField1($t);
    return res;
}

;;
;; Routing of a Contract C7gt
;;

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'f1) = $C7gt$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) { return (); }
    throw(130);
}
