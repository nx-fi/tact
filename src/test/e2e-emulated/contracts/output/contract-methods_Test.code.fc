#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; contract-methods_Test.headers.fc
;;
;; Header files for Test
;; NOTE: declarations are sorted for optimal order
;;

;; $Test$_store
builder $Test$_store(builder build_0, (int, int) v) inline;

;; $Test$_load
(slice, ((int, int))) $Test$_load(slice sc_0) inline;

;; $Test$_contract_init
(int, int) $Test$_contract_init() impure inline;

;; $Test$_contract_load
(int, int) $Test$_contract_load() impure inline;

;; $Test$_contract_store
() $Test$_contract_store((int, int) v) impure inline;

;; $Test$_fun_check
((int, int), int) $Test$_fun_check((int, int) $self) impure inline_ref;

;; $Test$_fun_checkImmediateReturn
((int, int), int) $Test$_fun_checkImmediateReturn((int, int) $self) impure inline_ref;

;; $Test$_fun_checkReturnViaLocalVar
((int, int), int) $Test$_fun_checkReturnViaLocalVar((int, int) $self) impure inline_ref;


;; contract-methods_Test.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

;; contract-methods_Test.storage.fc
;;
;; Type: Test
;; TLB: _ flags:uint256 oneMoreVar:int257 = Test
;;

builder $Test$_store(builder build_0, (int, int) v) inline {
    var (v'flags, v'oneMoreVar) = v;
    build_0 = build_0.store_uint(v'flags, 256);
    build_0 = build_0.store_int(v'oneMoreVar, 257);
    return build_0;
}

(slice, ((int, int))) $Test$_load(slice sc_0) inline {
    var v'flags = sc_0~load_uint(256);
    var v'oneMoreVar = sc_0~load_int(257);
    return (sc_0, (v'flags, v'oneMoreVar));
}

(int, int) $Test$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$Test$_load();
    } else {
        return $Test$_contract_init();
    }
}

() $Test$_contract_store((int, int) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $Test$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract Test functions
;;

(int, int) $Test$_contract_init() impure inline {
    var (($self'flags, $self'oneMoreVar)) = (0, 239);
    return ($self'flags, $self'oneMoreVar);
}

((int, int), int) $Test$_fun_check((int, int) $self) impure inline_ref {
    var (($self'flags, $self'oneMoreVar)) = $self;
    $self'flags = 42;
    var $fresh$ret_274 = true;
    return (($self'flags, $self'oneMoreVar), $fresh$ret_274);
}

((int, int), int) $Test$_fun_checkImmediateReturn((int, int) $self) impure inline_ref {
    var (($self'flags, $self'oneMoreVar)) = $self;
    var $fresh$ret_272 = ($self'flags, $self'oneMoreVar)~$Test$_fun_check();
    return (($self'flags, $self'oneMoreVar), $fresh$ret_272);
}

((int, int), int) $Test$_fun_checkReturnViaLocalVar((int, int) $self) impure inline_ref {
    var (($self'flags, $self'oneMoreVar)) = $self;
    int $foo = ($self'flags, $self'oneMoreVar)~$Test$_fun_check();
    var $fresh$ret_273 = $foo;
    return (($self'flags, $self'oneMoreVar), $fresh$ret_273);
}

;;
;; Get methods of a Contract Test
;;

;; message opcode reader utility
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";
;;
;; Routing of a Contract Test
;;

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'flags, $self'oneMoreVar) = $Test$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) { return (); }
    int op = 0;
    int in_msg_length = slice_bits(in_msg);
    if (in_msg_length >= 32) {
        op = in_msg~load_uint(32);
    }
    ;; Receive empty message
    if ((op == 0) & (in_msg_length <= 32)) {
        ($self'flags, $self'oneMoreVar)~$Test$_fun_checkImmediateReturn();
        throw_unless(37609, ($self'flags == 42));
        $self'flags = 0;
        ($self'flags, $self'oneMoreVar)~$Test$_fun_checkReturnViaLocalVar();
        throw_unless(31465, ($self'flags == 42));
        ;; Persist state
        $Test$_contract_store(($self'flags, $self'oneMoreVar));
        return ();
    }
    ;; Throw if not handled
    throw(130);
}

() __tact_selector_hack_asm() impure asm """
@atend @ 1 {
    execute current@ context@ current!
    {
        }END> b>
        
        <{
            SETCP0 DUP
            IFNOTJMP:<{
                DROP over <s ref@ 0 swap @procdictkeylen idict@ { "internal shortcut error" abort } ifnot @addop
            }>
swap <s ref@
            0 swap @procdictkeylen idict- drop
            -1 swap @procdictkeylen idict- drop
            65535 swap @procdictkeylen idict- drop

            @procdictkeylen DICTPUSHCONST DICTIGETJMPZ 11 THROWARG
        }> b>
    } : }END>c
    current@ context! current!
} does @atend !
""";
() __tact_selector_hack() method_id(65535) {
    return __tact_selector_hack_asm();
}