#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; address-injectivity_C10o.headers.fc
;;
;; Header files for C10o
;; NOTE: declarations are sorted for optimal order
;;

;; $C10o$_load
(slice, ((int, int))) $C10o$_load(slice sc_0) inline;

;; $C10o$init$_load
(slice, ((int))) $C10o$init$_load(slice sc_0) inline;

;; $C10o$_contract_init
(int, int) $C10o$_contract_init(int $v) impure inline;

;; $C10o$_contract_load
(int, int) $C10o$_contract_load() impure inline_ref;

;; $C10o$_fun_incrAndGetField1
((int, int), int) $C10o$_fun_incrAndGetField1((int, int) $self, int $n) impure inline_ref;

;; $C10o$_fun_getField2
((int, int), int) $C10o$_fun_getField2((int, int) $self) impure inline_ref;


;; address-injectivity_C10o.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

;; address-injectivity_C10o.storage.fc
;;
;; Type: C10o
;;

(slice, ((int, int))) $C10o$_load(slice sc_0) inline {
    var v'f2 = sc_0~load_int(257);
    var v'f1 = sc_0~load_int(257);
    return (sc_0, (v'f2, v'f1));
}

(slice, ((int))) $C10o$init$_load(slice sc_0) inline {
    var v'v = sc_0~load_int(257);
    return (sc_0, (v'v));
}

(int, int) $C10o$_contract_load() impure inline_ref {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$C10o$_load();
    } else {
        (int $v) = $sc~$C10o$init$_load();
        $sc.end_parse();
        return $C10o$_contract_init($v);
    }
}

;;
;; Contract C10o functions
;;

(int, int) $C10o$_contract_init(int $v) impure inline {
    var (($self'f2, $self'f1)) = (100, null());
    $self'f1 = $v;
    return ($self'f2, $self'f1);
}

((int, int), int) $C10o$_fun_incrAndGetField1((int, int) $self, int $n) impure inline_ref {
    var (($self'f2, $self'f1)) = $self;
    var $fresh$ret_127 = ($self'f1 + $n);
    return (($self'f2, $self'f1), $fresh$ret_127);
}

((int, int), int) $C10o$_fun_getField2((int, int) $self) impure inline_ref {
    var (($self'f2, $self'f1)) = $self;
    var $fresh$ret_128 = $self'f2;
    return (($self'f2, $self'f1), $fresh$ret_128);
}

;;
;; Get methods of a Contract C10o
;;

_ %incrAndGetField1(int $n) method_id(111350) {
    int $n = $n;
    var self = $C10o$_contract_load();
    var res = self~$C10o$_fun_incrAndGetField1($n);
    return res;
}

_ %getField2() method_id(83228) {
    var self = $C10o$_contract_load();
    var res = self~$C10o$_fun_getField2();
    return res;
}

;;
;; Routing of a Contract C10o
;;

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'f2, $self'f1) = $C10o$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) { return (); }
    throw(130);
}
