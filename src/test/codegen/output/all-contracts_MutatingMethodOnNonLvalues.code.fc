#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; all-contracts_MutatingMethodOnNonLvalues.headers.fc
;;
;; Header files for MutatingMethodOnNonLvalues
;; NOTE: declarations are sorted for optimal order
;;

;; $MutatingMethodOnNonLvalues$_contract_init
tuple $MutatingMethodOnNonLvalues$_contract_init() impure inline;

;; $MutatingMethodOnNonLvalues$_contract_load
tuple $MutatingMethodOnNonLvalues$_contract_load() impure inline_ref;

;; $MutatingMethodOnNonLvalues$_contract_store
() $MutatingMethodOnNonLvalues$_contract_store(tuple v) impure inline;

;; $Int$_fun_inc
(int, ()) $Int$_fun_inc(int $self) impure inline_ref;

;; $Int$_fun_inc$not_mut
() $Int$_fun_inc$not_mut(int $self) impure inline_ref;


;; all-contracts_MutatingMethodOnNonLvalues.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

;; all-contracts_MutatingMethodOnNonLvalues.native.fc
;; an empty FunC file to test imports of files with .func extension

;; all-contracts_MutatingMethodOnNonLvalues.storage.fc
;;
;; Type: MutatingMethodOnNonLvalues
;;

tuple $MutatingMethodOnNonLvalues$_contract_load() impure inline_ref {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return null();
    } else {
        return $MutatingMethodOnNonLvalues$_contract_init();
    }
}

() $MutatingMethodOnNonLvalues$_contract_store(tuple v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    set_data(b.end_cell());
}

;;
;; Contract MutatingMethodOnNonLvalues functions
;;

tuple $MutatingMethodOnNonLvalues$_contract_init() impure inline {
    tuple $self = null();
    return $self;
}

(int, ()) $Int$_fun_inc(int $self) impure inline_ref {
    var ($self) = $self;
    $self = $self + 1;
    return ($self, ());
}

() $Int$_fun_inc$not_mut(int $self) impure inline_ref {
    return $self~$Int$_fun_inc();
}

;;
;; Get methods of a Contract MutatingMethodOnNonLvalues
;;

;;
;; Routing of a Contract MutatingMethodOnNonLvalues
;;

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var $self = $MutatingMethodOnNonLvalues$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) { return (); }
    int op = 0;
    int in_msg_length = slice_bits(in_msg);
    if (in_msg_length >= 32) {
        op = in_msg~load_uint(32);
    }
    ;; Empty Receiver and Text Receivers
    var text_op = slice_hash(in_msg);
    ;; Receive "foo" message
    if (text_op == 0xf93728f4e38e2fa7ea9d391ab581a4e9c184efdeee79e77db38f01c07a1c624b) {
        $Int$_fun_inc$not_mut(41);
        $Int$_fun_inc$not_mut(42);
        $Int$_fun_inc$not_mut(43);
        $Int$_fun_inc$not_mut(44);
        ;; Persist state
        $MutatingMethodOnNonLvalues$_contract_store($self);
        return ();
    }
    ;; Throw if not handled
    throw(130);
}
