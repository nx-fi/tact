import type { Options } from "../../config/parseConfig";
export declare const allInFolder: (folder: string, globs: string[], options?: Options) => Promise<void>;
export declare const allInFolderFunc: (folder: string, globs: string[]) => Promise<void>;
